java with mr morad :

	part 1 : poo

- poo : porgramation oriente objet 
-la poo se base sur 3 principe :

		1-encapsilation : la sucurite  (les donne protege de ext , seul les methode qui le droit de comunique avec att , et pour communique en utulise meth , les methode joue role interface )

		2-heritage : la reutulisation 

		3-polymorphisme : l'extensabilite ( il doit etre present le heritage . c'est la modification u la mis a jour sans revenir en code juste en le appel

		4-abstraction : cest lors qu'on fait un classe abstrait c-a-d il ne peux pas instencier des objet sert que pour heritage 










	les classe abstraite


- une classe qui contient ou moin une methode abstraite .

- une classe qui herite du classe abstraite doit obligatoirement redfinire tout les methode abstraite de sa classe mere.

- une classe qui refuse de redefinire les methode abstraite de sa classe mere doit etre abstraite .

- une classe abstrait n'est pas instansible

- la notion en oop sert a exigent un protocol ( un contrat de traivail pour les classe fille ) pour un but organisationelle

: en doit redefinir tout les methode , gardes les meme type ..









	-java heritage

- en heritage la classe si il herite d'une classe il peux redfinire la methode qui il herite .

en java ya pas heritage multiple se qui veux dire une classe herite d'une seul classe .

que ce que une interface :

	une interface c'est une classe qui avant java 8(2014) etait purment abstrait (sa veux dire tout c'est methode etait abstrait ) mais apres 

java 8 cette notion a change alors l'interface peux contenir  des methode concraite (static . default . private) , interface contient des attribut 

final static .

en resume : interface c'est une classe qui contient des attribut final static des methode purment abstrait , des methode concrait ( default , static

private)




	en heritage en constructeur en fait pas l'ancien methode en utulise super(att mere )+this.fille;

	une classe qui contient une methode abbstrait doit etre sa claasse abstrait .

	interface  les methode na pas besoin de code cest finale , et meme les attribut mais seson final ms il faut la fair prive ou static ou par default .

		public default void ,,,(){..code..} , redefinition en doit pas changer les attributs .




		interface :
une interface peut contenir que les attribut est est les methode son defenition mais il a avantage que il peut etre implemente par plusieur classe a la meme fois 

		classe abstrait :
une classe qui a la posivilite d'avoir des attribut des methode avec des definition ou sans . mais il y'a le probleme de diammant 









			les execeptios 
 		
	-try : en ecrir les code des execute est si rencontre error il passe a finally .

			en ecrire les code sustisible .
	
	-catch :quand il touve error il entre en try est il arrete .


	- finally : toujour execute le code meme si il trouve error.
		

try : J’essaie d’exécuter un code qui peut planter.

catch : Si ça plante avec une certaine erreur, je fais ceci au lieu de laisser le programme s’arrêter.

finally (optionnel) : Dans tous les cas (plante ou pas), je fais ce nettoyage (ex. fermer un fichier).


	en peut fait un try avec plusieur catch . catch ou en fait les exception est vas le choisir automatiquement java , en peut meme faire des classe de execption .


